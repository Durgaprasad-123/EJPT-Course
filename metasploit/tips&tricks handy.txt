service postgresql start
workspace -a <name>
msfconsole -q
based on the ports we can use the modules and 
search name
use 
set the RHOSTS and remaining get the target 
sometimes we need privileges
getprivs
SeImpersonatePrivilege.
Used privilege escalation via:
"getsystem"
we can esclate privileges
to convert from shell to meterpreter if we have a direct shell access ctrl + z
then search for shell_to_meterpreter
otherwise sessions -u <session id > to convert into meterpreter
set lhost session boom
rsync rsync://<target ip>>
rsync -av rsync://<ip>/dir . //to copy file to here

tip:observe the webpages it helps for the which one should i use like that
